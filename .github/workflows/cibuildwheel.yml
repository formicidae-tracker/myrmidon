name: cibuildwheel
on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - master
  release:
    types: published

jobs:
  snap-fort-studio:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-tags: true

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
      - run: pip install build
      - name: Build sdist
        run: pip -m build --sdist
        working-directory: bindings/python

      - uses: pypa/cibuildwheel@v3.0.1
        with:
          package-dir: bindings/python/dist/*.tar.gz

      - uses: actions/upload-artifact@v4
        with:
          name: cib
